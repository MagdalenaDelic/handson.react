import { colors } from "../../lib/style/theme";
import styled from "styled-components";

export const SingleCourseText = styled.div`
  font-size: 16px;
  line-height: 180%;
  color: ${colors.textSecondary};
`;

export const SingleCourseP = styled.p``;
